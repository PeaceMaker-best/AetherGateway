# Optional reverse proxy for LAN or HTTPS deployments.
# Default docker compose usage does not need Caddy.
#
# Example:
#   aethergateway.local {
#     import aethergateway_proxy
#   }
#
# For plain LAN HTTP while testing:
#   :8080 {
#     import aethergateway_proxy
#   }

(aethergateway_proxy) {
  encode zstd gzip

  reverse_proxy dashboard:8080
}
